info:
  name: find rain-mob metadata (in ttl)
  type: http
  seq: 1

http:
  method: GET
  url: "{{serviceHost}}?query=rain+mobility"
  params:
    - name: query
      value: rain+mobility
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
