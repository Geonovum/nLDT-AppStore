info:
  name: find rain-mob metadata (in json)
  type: http
  seq: 2

http:
  method: GET
  url: "{{serviceHost}}/rain-mobility.json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
